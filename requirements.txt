# SPDX-FileCopyrightText: Copyright (c) 2025 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

# Solar - All Dependencies
# Install with: pip install -r requirements.txt

# ROCm wheels are not published on the default PyPI index. Keep both official
# PyTorch indexes here so the documented direct pip command is self-contained.
--extra-index-url https://download.pytorch.org/whl/rocm7.2
--extra-index-url https://download.pytorch.org/whl/

# Core
torch==2.11.0+rocm7.2; sys_platform == 'linux' and platform_machine == 'x86_64'
triton-rocm==3.6.0; sys_platform == 'linux' and platform_machine == 'x86_64'
networkx>=3.0
pyyaml>=6.0
numpy>=1.24.0
matplotlib>=3.5.0

# Solar Analysis & Verification
scipy>=1.10.0

# Data Processing
pandas>=1.5.0
pyarrow>=14.0.0
einops>=0.6.0

# HuggingFace (for model download)
huggingface-hub>=0.16.0
safetensors>=0.3.0

# LLM Agent (optional - for auto-generating einsum handlers)
openai>=1.0.0

# Graph Visualization (optional - for PDF graph rendering)
graphviz>=0.20

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
